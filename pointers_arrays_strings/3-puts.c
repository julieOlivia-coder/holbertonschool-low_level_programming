#include "main.h"
#include <stdio.h>

/**
 * _puts(char *str) - function that prints a string
 * @*str : string pointé
 * Return: void
 */
void _puts(char *str);

{
		for (; *str != '\0'; str++)
		_putchar str;
}
